# Vacío: backend y frontend se sirven desde el mismo contenedor/origen (HF Spaces),
# por lo que el frontend usa rutas relativas y no hace falta editar este valor
# para cada despliegue.
VITE_API_URL=
